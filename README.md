

## Poker Dev Server (fake-money)
Start the local WebSocket server (broadcast table actions):
```bash
npm run server
```
It listens on `ws://localhost:8787`. The Poker page will auto-connect and show a live "Table Feed".
Open two browser tabs to see actions broadcast between them.
